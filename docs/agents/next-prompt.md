# Next-session prompt — Issue #12 (Repositioned marketing homepage)

Paste the block below to start the next session.

---

Implement issue #12 in adamw-af/pubflow: **"v2: Repositioned marketing homepage"**. Read the full issue with `gh issue view 12`, and skim PRD #1 (`gh issue view 1`) for v2 context — especially User Stories 40–46 (Homepage & finished-feel) and the "Homepage" implementation decisions. Read `CONTEXT.md` for the domain language you must match (**Workspace**, **Trial**, **Social Account**, **Platform**) and the canonical pricing facts (Base $15 / 25 accounts, Pro $20 / 50, Premium $60 / unlimited; unlimited posts on every tier). **Branch from `main` after fetching** (#11 lands just before this). This is a marketing/UI + copy task — you almost certainly **won't need a new ADR** (no new architecture), but write one if you make a structural call.

**Why this next:** #11 (onboarding + composer polish) is done, which closes out the value-first funnel. #12 is now the **only remaining `ready-for-agent` coding issue** — its blocker #3 (legal/trust pages) is already merged (the `/privacy`, `/terms`, `/data-deletion`, `/contact` routes exist and the footer links resolve). Everything else open is `ready-for-human` ops (#13 Meta review, #14 TikTok audit, #15 Google/YouTube verification) — app-review submissions you can't do from code. So the homepage is the last build task before v2 is feature-complete.

**The bar is the "finished-feel" gate, not `ccdesign`.** Per the PRD, the homepage AC is the finished-feel checklist (mobile-first, zero dead links, information-complete, no console/render bugs), **not** the `ccdesign` adherence lint (that gate is for the composer/onboarding). Keep the page visually consistent with the existing design tokens, but don't block on `ccdesign` — and note it's gitignored and may not be installed anyway. This is **not a /tdd task**: per the PRD's Testing Decisions the homepage gets no component or E2E tests and no automated dead-link checker — manual QA (run the app, eyeball mobile + logged-out, click every link) is the bar. You should **not** be changing backend behaviour, so don't add `convex-test` cases.

**Current state — what's already there (verify before assuming, don't rebuild from scratch):**
- **The homepage is substantially built** (`app/routes/home.tsx`) on the full design system: a hero, an all-8 `ChannelStrip`, three `FeatureRow`s, a dark `FlowBand` stat strip, a `Pricing` section, and a `FinalCTA`. Site chrome (`app/components/site/site-chrome.tsx`) has a `SiteNav` and a `SiteFooter` whose **legal links already resolve** to the real routes — AC#5 (zero dead links) is largely met; **confirm it, don't redo it.**
- **Pricing already renders three tiers**, but it pulls amounts from **Polar live data** (`getAvailablePlans`) and falls back to `"—"` when Polar isn't configured. A logged-out visitor on an unconfigured environment would see dashes instead of prices — a finished-feel + AC#2 risk. Make concrete numbers **resilient**: the canonical $15/$20/$60 + 25/50/unlimited-accounts + unlimited-posts facts must show even if the Polar fetch returns nothing.

**What's missing (the actual work):**
- **The dated, named-competitor comparison table on accounts-per-dollar (AC#3)** is **entirely absent** — this is the headline deliverable. Build an honest comparison: PubFlow's accounts-per-dollar vs. **named** competitors (e.g. Buffer, Hootsuite, Later, Sprout — pick real, current ones), with a visible **"as of <date>"** stamp and accurate figures. **No bare "best price on the market" superlative** — let the dated numbers make the case. Treat competitor pricing as volatile: research current figures, cite the date, and if you can't verify a number, leave it out rather than guess.
- **Hero repositioning to simplicity-as-the-promise (AC#1).** Today's hero ("Schedule once. Post everywhere.") leads on multi-channel breadth, not on *simplicity*. Reposition the emotional lead so simplicity is the core promise (per US#40 and the PRD's "lead emotionally with simplicity"); keep price as the proof (AC#2/US#41).
- **Finished-feel hardening (AC#4/#6).** Eyeball every breakpoint for mobile usability, and hunt console/render bugs.

**Don't pull scope forward:** keep this to the homepage and its pricing/comparison surfaces. The legal pages are #3 (done) — don't rewrite them. Don't touch onboarding/composer/dashboard. New platforms and the app-review submissions are their own issues (#13–15).

**Open questions to surface early (flag, don't block):**
- **External icon CDN = broken-images risk (AC#6).** `ChannelBubble` loads platform marks from `cdn.jsdelivr.net/npm/simple-icons@13/...` at runtime. If the environment's network policy blocks that CDN, every channel icon renders broken — directly failing "no broken images." Strongly consider swapping to the local `platformIcon` helper (`~/lib/platform-icons`, no network) for reliability, and flag the trade-off (simple-icons gives brand-accurate marks; the local set is lucide-based).
- **Fabricated social-proof numbers vs. the honesty mandate.** The page currently asserts "Loved by 12,000+ creators", "3.4M posts published", etc. For a public artifact the PRD wants to read as *honest and trustworthy*, invented metrics are a liability. Flag whether to keep them, soften to qualitative copy, or cut them — get a call rather than shipping unverifiable stats next to an "honest comparison" table.
- **Competitor-data accuracy & dating.** Prices drift; an out-of-date or wrong figure undermines the whole "honest comparison" play. Date-stamp the table and note that the figures need a periodic re-check. If web research isn't available in the environment, say so and use clearly-dated known figures with a "verify before launch" note.
- **Pricing source of truth.** Decide whether the homepage should trust Polar live data at all for the logged-out marketing view, or hard-code the canonical tier facts (and use Polar only inside the authenticated checkout). Lean toward concrete, always-present numbers for the marketing page.

**Verification:** `npm run typecheck` clean; `npm test` still green (you shouldn't be changing backend behaviour, so the existing suite must stay passing); `npm run build` succeeds. Then **run the app and eyeball the logged-out homepage**: simplicity reads as the promise; concrete prices + the dated competitor table are present and accurate; the layout holds at mobile widths; every nav/footer/CTA link resolves (no dead links); no console errors, broken images, or half-rendered sections. When done, commit and open a PR; note the competitor figures' "as of" date and how you resolved the icon-CDN and social-proof-honesty questions.
