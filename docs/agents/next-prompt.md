# Next-session prompt — Re-land Issue #10 (YouTube Shorts platform — merged PR was lost from `main`)

Paste the block below to start the next session.

---

Implement and close the **YouTube Shorts platform** (issue #10, **reopened**) in adamw-af/pubflow. This is a **recovery task, not a green-field build**: #10 was implemented and its PR (#24) was merged, but the code was **dropped from `main` by a later history rewrite** and is missing today — which is why the issue was reopened. Read #10 with `gh issue view 10` and PR #24 (`gh pr view 24`) for the full design write-up. Read `CONTEXT.md` (**Platform**, **Platform Capability**, **Post Variant**, **Publication**) and `docs/adr/0006-platform-adapter-registry.md` + `docs/adr/0007-async-video-publishing-via-cron-polling.md` — YouTube is the **second consumer of the async video pipeline** built for TikTok (#8), so the rule is *reuse, don't re-derive*. **Branch from `main` after fetching.**

**The discrepancy — verify it yourself first (don't assume):**
- GitHub records **PR #24 as merged** (`merged_at` is set) and #10 was closed on the strength of it — yet **the YouTube adapter is not in `main`** (the issue has since been reopened for exactly this reason). Confirm for yourself: there is **no `convex/platforms/youtube.ts`**, and `convex/platforms/metadata.ts` `PLATFORM_IDS` stops at `tiktok` (no `youtube`). The history shows why — after #24 merged onto the #23 (TikTok) merge commit `8fc2ebc`, `main` continued from a sibling commit (`4696a7c` "prompt doc") that does **not** contain #24, so #24's commit `c088df7` is **not an ancestor of `main`**. The work was silently lost.
- **The code is fully recoverable.** The original branch still exists on the remote: **`issue-10-youtube-shorts` @ `c088df7a62baa625e0920dc5774ddb63e5a61985`**. Fetch it and use it as the **source of truth** for what to re-land — `git fetch origin issue-10-youtube-shorts`. Do **not** rebuild from scratch.

**Why this next:** With #11 done (PR #28), **every other open issue is human-owned ops** — #13 (Meta review), #14 (TikTok audit), #15 (Google/YouTube verification) — which can't be done from code. YouTube Shorts is therefore the **only remaining agent-codeable v2 scope gap**, it's an in-scope Wave 2 Platform per PRD #1, and #15 (the Google/YouTube OAuth verification) is blocked on having a **demo-able YouTube adapter** — which currently doesn't exist. Re-landing #10 is what actually completes v2's build scope and unblocks #15.

**What PR #24 added (the 15-file changeset to recover — `git diff 8fc2ebc c088df7`):**
- **`convex/platforms/youtube.ts`** (new, ~248 lines) — Google OAuth (`youtube.upload` + `youtube.readonly`, `access_type=offline`; Google doesn't rotate refresh tokens, so `refreshToken()` returns none and the publisher keeps the stored one). Unlike TikTok's `PULL_FROM_URL`, YouTube has no pull source: `publish()` streams the bytes **from the R2 URL server-side**, opens a **resumable** upload session (`videos.insert?uploadType=resumable`), reads the session URI from the `Location` header, `PUT`s the bytes, and returns the **video id as the async job handle**. `checkStatus()` polls `videos.list?part=status,processingDetails` → `published` / `failed` (with reason) / in-progress. This all rides the existing durable `publishing` state + `pollPublishingPublications` cron **without changing the state machine**.
- **`convex/platforms/youtube.test.ts`** (new, ~332 lines) — adapter contract tests (request building, `PublishResult`/status mapping incl. the in-progress shape), plus capability + registry-roster tests.
- **Capability + validation** — `youtube` descriptor (`videoRequired` + `titleRequired`, 9:16, ≤180s). `titleRequired` was a dead flag; the branch adds a `title_required` rule to `validateAgainstCapability` (and `title` on `VariantInput`) so a titleless Short is rejected inline **and** at schedule time, exactly like `video_required`.
- **Options threading** — `VariantOptions.youtube = { title }` mirroring the `tiktokOptions` pattern: `youtubeOptions` validator + schema fields on `postVariants`/`postTemplateVariants`, threaded through `posts.ts` and `publisher.ts`.
- **Composer** — YouTube icon + a **title input** shown when `capability.titleRequired`; video-required is already enforced by the Capability pipeline; connect/onboarding/settings lists derive from the registry automatically.

**The actual work — recover + reconcile with today's `main` (this is the real effort):**
`main` has moved on since `8fc2ebc`, and several files the YouTube branch touches were **also changed** by later merges — so a clean cherry-pick will conflict. Re-apply #24's work and reconcile against current `main`, specifically:
- **`app/components/composer/PostComposer.tsx`** — rewritten since (registry-driven `PlatformBubble`, plus #11's `Input`/token polish in PR #28). Re-introduce the YouTube **title field** wiring on top of the current file, don't revert the newer composer.
- **`app/lib/platform-icons.tsx`** — note `youtube` is already in `BRAND_COLOR_IDS` and there's a `--ch-youtube` token, **but `ICONS` has no `youtube` entry** (it would render `null`). Add the YouTube icon (PR #24 added it; match the current icon-map style).
- **`convex/platforms/metadata.ts`** — add `youtube` to `PLATFORM_IDS` + `PLATFORM_METADATA` (registry now has 7 platforms, not the branch's older roster).
- **`convex/schema.ts`, `convex/posts.ts`, `convex/publisher.ts`, `convex/platforms/{types,registry,capabilityValidation}.ts`** — re-apply the `youtubeOptions`/`title_required`/registry-entry changes onto current versions.
- **Registry/metadata roster tests** (`registry.test.ts`, `metadata.test.ts`) pin the platform list — update them to include `youtube`.

**Out of scope:** don't touch the other platforms, the homepage (#12), onboarding/composer beyond the YouTube title wiring, or the Trial funnel. **Do not attempt #15** (Google/YouTube OAuth **verification** is human-owned ops — account setup, consent screen, submission); this task only makes the adapter exist and be demo-able. This **is** a `/tdd`-appropriate platform task per the PRD's testing decisions: the adapter contract test + capability validation are the seams (recover them from the branch), and live public posting stays gated on #15 (an unverified Google app forces uploads to `private` regardless of requested `privacyStatus`). New env: `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`.

**Flag early / open questions:**
- **#10 is already reopened — close it when this lands.** Treat PR #24's "merged" state as a false positive caused by the history rewrite; your new PR should `Closes #10` so the issue is closed only once the code is genuinely back in `main`.
- **How the loss happened.** Worth a one-line note so it isn't repeated: a branch (`4696a7c`) was cut from `8fc2ebc` in parallel with #24 and `main` followed that line, orphaning the #24 merge. No action needed beyond re-landing, but call it out.

**Verification:** `npm run typecheck` clean; `npm test` green (PR #24 reported **111** tests on its base; current `main` is **101** — after re-landing, expect roughly `current + the YouTube/capability/roster cases`, all passing); `npm run build` succeeds. A live OAuth/upload run isn't possible here (no Google creds in the web env) — verify via the recovered adapter contract tests + typecheck/build, and say so plainly in the PR. Commit, push, and open a PR that `Closes #10`, explains this was a **recovery of lost merged work**, and notes #15 remains the operator's verification step.

**After this:** with YouTube Shorts back in `main`, **v2's agent-codeable scope is genuinely complete** — every Platform in the PRD ships. What remains is solely the human-owned app-review ops: **#13 (Meta), #14 (TikTok), #15 (Google/YouTube)**. The next handoff should point at those submissions, not another build task.
