# Next-session prompt — Issue #11 (Rebuilt onboarding wizard + composer polish)

Paste the block below to start the next session.

---

Implement and close issue #11 in adamw-af/pubflow: **"Rebuilt onboarding wizard + composer polish"**. Read the full issue with `gh issue view 11`, and skim PRD #1 (`gh issue view 1`) — especially **User Stories 35–39** (onboarding + composer polish) and the "Frontend / polish" implementation decisions, where **"polished" is defined as passing the `ccdesign` adherence lint**, not a subjective call. Read `CONTEXT.md` for the domain language you must match (**Workspace**, **Trial**, **Social Account**, **Platform**, **Platform Capability**, **Post Variant**) and `docs/adr/0006-platform-adapter-registry.md` + `docs/adr/0008-trial-funnel-and-workspace-access.md` for the registry and Trial-access patterns these surfaces lean on. **Branch from `main` after fetching** (#12, the repositioned homepage, lands just before this — PR #27).

**Why this next:** #12 (homepage) is shipped, which closes the last finished-feel marketing task. #11 is now the **only remaining `ready-for-agent` coding issue**, and its blockers are all merged: #2 (adapter registry), #4 (encrypted tokens), #5 (Platform Capability validation), plus #8's Trial funnel. Everything else open is `ready-for-human` ops — #13 (Meta app review), #14 (TikTok audit), #15 (Google/YouTube verification) — app-review submissions you can't do from code. **So #11 is the final v2 build/polish task before the agent-codeable scope is done.**

**Current state — substantially built (verify before assuming; do NOT rebuild from scratch):**
- **Onboarding is already registry-driven and Trial-aware.** `app/components/onboarding/OnboardingWizard.tsx` lists **every** Platform from `platformMetadata` dynamically (no hardcoded three — **AC#1 met**), shows the **7-day Trial countdown** and the **free-limit notice / disabled connect tiles** once the one free Social Account is connected, driven by `getWorkspaceAccess` (`canConnectAnotherAccount` / `reason`), mirroring `settings`. That's **AC#2 (Trial state) met**. Confirm both by running the app — don't redo them.
- **The composer is built and registry-driven.** `app/components/composer/PostComposer.tsx` (+ `VariantEditor.tsx`, `CharacterCounter.tsx`) renders brand chips via `platformIcon`/`platformBrandColor` (no per-platform maps that miss new platforms), does **inline per-Platform Capability validation** through the same `validateAgainstCapability` the backend uses, with per-Platform tabs, a sync-all-content toggle, TikTok privacy/disclosure options, required-video enforcement, and a schedule picker.

**The actual remaining work — the polish gate (AC#3 + AC#4):**
- **AC#3 is "the composer passes the `ccdesign` adherence lint."** That is the open criterion. AC#4 is "onboarding and composer are visually consistent with the design system." Both are about adherence, not new features.
- **`ccdesign` reality — flag and handle, don't pretend.** `ccdesign` is **gitignored and is NOT installed in the web execution environment** (confirmed: no `ccdesign` binary, `.gitignore` excludes it). So:
  - **If `ccdesign` is available** (installed/runnable in your environment): run the adherence lint over the composer + onboarding surfaces and **fix every finding until it passes**; paste the clean output in the PR.
  - **If it is NOT available:** do a **rigorous manual adherence pass** instead — prefer the `app/components/ui` primitives and the `--*` design tokens in `app/app.css` over hand-rolled raw Tailwind colours/spacing wherever a token or component exists; match the patterns already shipped in settings/dashboard and the new homepage (#12). Then **run the app and eyeball the composer** at: single account, multiple accounts (tabs + the per-tab error badge), a **video-only Platform** (TikTok / YouTube Shorts — required-video + TikTok privacy options), and **mobile widths**. **Explicitly state in the PR that the formal `ccdesign` lint could not be run here and must be run where it's installed before AC#3 is signed off — do not claim the lint passed if you didn't run it.**
- **Light touch.** These surfaces already use the design system; only fix **concrete** adherence/consistency gaps — don't churn working code or restyle for taste. If you find no real gaps, the honest deliverable is a **verification pass** plus a note recommending the operator run `ccdesign` to formally close AC#3.

**Don't pull scope forward / out of scope:** keep this to **the onboarding wizard and the composer**. The **branded Clerk sign-in/sign-up pages** (US#34) are a separate concern — `app/routes/sign-in.tsx` / `sign-up.tsx` exist already — and are **not** in #11's ACs; don't rebuild them here. Don't touch the homepage (#12, done), don't add Platforms, and don't attempt the app-review ops (#13–15). Per the PRD's Testing Decisions this is **not a /tdd task**: composer/onboarding get **no** component or E2E tests. You should **not** be changing backend behaviour, so **don't add `convex-test` cases** — and the existing suite must stay green.

**Open questions to surface early (flag, don't block):**
- **`ccdesign` availability** (above) — decide and state which path you took.
- **Is there any real adherence gap at all?** It's plausible #11 is effectively complete and just needs verification + closing. If so, say so plainly rather than inventing churn — and recommend whether to close #11 or leave AC#3 pending the formal lint.

**Verification:** `npm run typecheck` clean; `npm test` still green (101 tests — you shouldn't be changing backend behaviour, so the suite must stay passing); `npm run build` succeeds. Then **run the app**: walk onboarding end to end (Workspace → Connect → Ready, with the Trial countdown and the free-limit state) and exercise the composer across single-account, multi-account, video-only, and mobile. When done, commit and open a PR; note how you resolved the `ccdesign`-availability question and whether you recommend closing #11.

**After this issue:** #11 is the last `ready-for-agent` coding issue — once it's merged, **v2's agent-codeable scope is complete**, and only the human-owned ops issues (#13 Meta, #14 TikTok, #15 Google/YouTube) remain. Reflect that in the handoff: rather than pointing the next session at another build task, note that what's left is app-review submissions the operator owns.
