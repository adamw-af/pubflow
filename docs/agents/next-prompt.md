# Next-session prompt — Issue #11 (Trial-aware onboarding + composer polish)

Paste the block below to start the next session.

---

Implement issue #11 in adamw-af/pubflow: **"v2: Rebuilt onboarding wizard + composer polish"**. Read the full issue with `gh issue view 11`, and skim PRD #1 (`gh issue view 1`) for v2 context — especially User Stories 34–39 (Signup & onboarding polish) and the "Frontend / polish" implementation decisions. Read `CONTEXT.md` for the domain language you must match (**Workspace**, **Trial**, **Platform**, **Platform Capability**, **Social Account**, **Post Variant**). The structural decisions you depend on already have ADRs — **0006** (platform adapter registry) and **0008** (Trial funnel + `getWorkspaceAccess`); read both. You almost certainly won't need a new ADR here (this is UI polish, not new architecture), but write one if you make a structural call. **Branch from `main` after fetching** (`main` advances as #4 merges).

**Why this next:** #4 (Trial funnel + branded auth) is done and was #11's last open blocker (#2 registry and #5 Capability/composer are already merged). #11 is the **critical-path** pick — it's the onboarding/composer polish that turns the value-first funnel into a finished first-run. #12 (homepage) is also unblocked and ready-for-agent but off the critical path.

**The definition of "polished" is not subjective.** The AC is explicit: *"The composer passes the `ccdesign` adherence lint."* `ccdesign` is a locally-installed tool (it's in `.gitignore`, not committed) — **confirm it's available first** and run it as the gate. This is a **design-adherence + UX task, not a /tdd task**: per the PRD's Testing Decisions, onboarding/composer get **no component or E2E tests** — the `ccdesign` lint plus manual QA (run the app) is the bar. Only write `convex-test` cases if you end up changing backend behaviour (you probably won't).

**Current state — what's already there (verify before assuming):**
- **Onboarding** (`app/components/onboarding/OnboardingWizard.tsx`) **already iterates the registry** (`platformMetadata` from `convex/platforms/metadata.ts`), so AC#1 ("lists every registered Platform dynamically") is largely met — *confirm it, don't rebuild from scratch.* What's **missing is Trial-awareness** (AC#2): it shows no Trial state and doesn't respect the one-account free limit.
- **Composer** (`app/components/composer/PostComposer.tsx`, `VariantEditor.tsx`, `CharacterCounter.tsx`) is Capability-driven and works, but has **design-consistency gaps** — e.g. `PostComposer` hardcodes `PLATFORM_ICONS`/`PLATFORM_COLORS` maps that only cover **4 of the 7** registered platforms (linkedin/instagram/x/tiktok — missing bluesky/facebook/threads), instead of using the shared `platformIcon` helper (`~/lib/platform-icons`, covers all 7) and the `--ch-*` brand-color tokens in `app/app.css`. A platform with no entry renders a blank/black chip.

**What to build:**

- **Trial-aware onboarding** (AC#2, US#37): wire `getWorkspaceAccess` (the deep module from #4, `convex/subscriptions.ts`) into the wizard. Surface the Trial state (e.g. "You're on a 7-day trial — N days left, no card needed") and respect the **one-Social-Account free limit on Trial**: once one account is connected, communicate that connecting more needs a subscription. **Mirror the pattern already shipped in `app/routes/dashboard/settings.tsx`** (the `atLimit` notice + disabled connect buttons driven by `access.canConnectAnotherAccount` / `access.reason`) so onboarding and settings stay consistent. The server already enforces this at `oauth.upsertSocialAccount` — the UI just reflects it.
- **Confirm dynamic platform list** (AC#1): the connect step must render every registered Platform from the registry (it does today via `platformMetadata`) — no hardcoded three. Just verify and keep it registry-derived.
- **Composer polish to pass `ccdesign`** (AC#3): replace the hardcoded `PLATFORM_ICONS`/`PLATFORM_COLORS` with the shared `platformIcon(iconKey)` helper and the `--ch-<platform>` CSS tokens so **all** registered platforms render consistently (account selector chips, tab triggers, the per-Platform validation badges). Then run the `ccdesign` adherence lint and fix what it flags.
- **Visual consistency** (AC#4): onboarding and composer should read as the same design system as the rest of the app (the branded auth pages from #4 and the dashboard chrome are good references for spacing, type scale, and the Flow Green primary).

**Don't pull scope forward:** keep this to onboarding + composer. The homepage is #12; new platforms are their own issues.

**Open questions to surface early (flag, don't block):**
- **YouTube (#10) is marked closed but is NOT on `main`.** The registry (`PLATFORM_IDS` in `metadata.ts`) lists 7 platforms **without `youtube`**, and there's no `convex/platforms/youtube.*`. The issue tracker shows #10 closed, so its PR may have merged after you branch, or been closed unmerged. **Re-check `main` at branch time:** if YouTube has landed, the composer must also handle its Capability (video + **title required**, US#15) consistently; if not, don't build YouTube UI — just make sure the icon/color/validation surfaces are registry-driven so YouTube slots in for free when it merges.
- **`ccdesign` availability.** It's gitignored and may not be installed in a fresh environment. If you can't run it, say so and fall back to careful manual adherence against the design tokens — but call out that the AC's lint gate couldn't be executed.
- **Composer drop-vs-warn interaction** (PRD open item, US#17): the exact UX for "one selected Platform can't accept this content while others can" is marked TBD in design. The backend already schedules the valid Platforms and skips the blocked one; today the composer blocks scheduling until errors are fixed. Don't redesign this interaction under the banner of "polish" without flagging it.

**Verification:** the `ccdesign` adherence lint passes on the composer (the AC gate); `npm run typecheck` clean; `npm test` still green (you shouldn't be changing backend behaviour, so the existing suite must stay passing). Run the app and eyeball: a brand-new user sees a Trial-aware onboarding that lists all registered platforms and enforces the one-account free limit; the composer renders every connected platform's icon/color consistently. When done, commit and open a PR; note whether `ccdesign` could be run, and call out the YouTube-on-`main` status you found.
